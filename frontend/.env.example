# Frontend .env.example – copy to .env and adjust as needed
# Vite only exposes variables prefixed with VITE_

# API base URL (leave empty when using Vite dev proxy to backend)
# VITE_API_BASE_URL=

# App title
# VITE_APP_TITLE=Image Converter

# Show raw errors in UI (development)
# VITE_DEBUG=false
